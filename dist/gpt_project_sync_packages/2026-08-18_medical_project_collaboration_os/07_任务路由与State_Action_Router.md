# 任务路由与 State Action Router

每次先写 task_type、current_state、responsibility_layer、allowed_changes、forbidden_changes、must_read 和 blocked_if。

状态只决定允许的下一动作，不替代商业判断。战略未锁定时只允许候选比较、事实整理和验证设计；需要路线选择时交还用户与 ChatGPT。
